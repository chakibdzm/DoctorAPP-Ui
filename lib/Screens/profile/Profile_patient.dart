import 'package:doctor/size_config.dart';
import 'package:flutter/material.dart';
import 'package:flutter_svg/flutter_svg.dart';
class ProfilePat extends StatefulWidget {
  const ProfilePat({Key? key}) : super(key: key);

  @override
  State<ProfilePat> createState() => _ProfilePatState();
}

class _ProfilePatState extends State<ProfilePat> {
  @override
  Widget build(BuildContext context) {
    SizeConfig().init(context);
    return Container()
  }
}
